[package]
name = "gamma-rs"
version = "0.1.0"
edition = "2024"

[dependencies]
gvas = "0.11.0"
indexmap = "2.14.0"

[lints.clippy]
todo = "warn"
unimplemented = "warn"
unwrap_used = "warn"
expect_used = "warn"
panic = "warn"

[alias]
clip = "clippy --all-targets --all-features"
